pnpm-lock.yaml
# its release-age exclusion list is one string that grows a version per
# gbz-base release, and past 80 columns prettier folds it on every one
pnpm-workspace.yaml
src/bandage/bandage-layout.js
# vendored OGDF, which `prettier --write .` would otherwise reformat: 26 md and
# yml files upstream owns, turning the next version bump into a diff against our
# own whitespace. See vendor/README.md.
vendor/
